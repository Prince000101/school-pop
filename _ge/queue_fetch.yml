decorators:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.decorators
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=600
  volumes:
    - decorators_data:/var/lib/data
  restart: unless-stopped
