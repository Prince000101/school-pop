metrics:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.metrics
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=600
  volumes:
    - metrics_data:/var/lib/data
  restart: unless-stopped
