migration:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.migration
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=60
  volumes:
    - migration_data:/var/lib/data
  restart: unless-stopped
