utils:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.utils
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=600
  volumes:
    - utils_data:/var/lib/data
  restart: unless-stopped
