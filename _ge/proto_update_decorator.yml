repo:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.repo
    args:
      NODE_ENV: production
  ports:
    - "4000:4000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=300
  volumes:
    - repo_data:/var/lib/data
  restart: unless-stopped
