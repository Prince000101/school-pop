session:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.session
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=60
  volumes:
    - session_data:/var/lib/data
  restart: unless-stopped
