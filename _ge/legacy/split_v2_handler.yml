query:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.query
    args:
      NODE_ENV: production
  ports:
    - "4000:4000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=300
  volumes:
    - query_data:/var/lib/data
  restart: unless-stopped
