logger:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.logger
    args:
      NODE_ENV: production
  ports:
    - "4000:4000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=600
  volumes:
    - logger_data:/var/lib/data
  restart: unless-stopped
