auth:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.auth
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=600
  volumes:
    - auth_data:/var/lib/data
  restart: unless-stopped
