exceptions:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.exceptions
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=3600
  volumes:
    - exceptions_data:/var/lib/data
  restart: unless-stopped
