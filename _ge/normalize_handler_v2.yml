token:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.token
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=120
  volumes:
    - token_data:/var/lib/data
  restart: unless-stopped
