helpers:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.helpers
    args:
      NODE_ENV: production
  ports:
    - "4000:4000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=300
  volumes:
    - helpers_data:/var/lib/data
  restart: unless-stopped
