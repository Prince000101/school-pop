validator:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.validator
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=120
  volumes:
    - validator_data:/var/lib/data
  restart: unless-stopped
