model:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.model
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=300
  volumes:
    - model_data:/var/lib/data
  restart: unless-stopped
