auth:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.auth
    args:
      NODE_ENV: production
  ports:
    - "4000:4000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=3600
  volumes:
    - auth_data:/var/lib/data
  restart: unless-stopped
