login:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.login
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=60
  volumes:
    - login_data:/var/lib/data
  restart: unless-stopped
