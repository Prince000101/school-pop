cache:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.cache
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=3600
  volumes:
    - cache_data:/var/lib/data
  restart: unless-stopped
