button:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.button
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=300
  volumes:
    - button_data:/var/lib/data
  restart: unless-stopped
