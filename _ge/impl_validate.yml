utils:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.utils
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=300
  volumes:
    - utils_data:/var/lib/data
  restart: unless-stopped
