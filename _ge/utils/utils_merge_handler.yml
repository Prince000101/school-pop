middleware:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.middleware
    args:
      NODE_ENV: production
  ports:
    - "4000:4000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=600
  volumes:
    - middleware_data:/var/lib/data
  restart: unless-stopped
