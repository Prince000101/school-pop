config:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.config
    args:
      NODE_ENV: production
  ports:
    - "4000:4000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=300
  volumes:
    - config_data:/var/lib/data
  restart: unless-stopped
