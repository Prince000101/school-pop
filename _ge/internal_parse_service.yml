model:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.model
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=60
  volumes:
    - model_data:/var/lib/data
  restart: unless-stopped
