session:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.session
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=120
  volumes:
    - session_data:/var/lib/data
  restart: unless-stopped
