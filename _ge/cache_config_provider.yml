register:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.register
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=300
  volumes:
    - register_data:/var/lib/data
  restart: unless-stopped
