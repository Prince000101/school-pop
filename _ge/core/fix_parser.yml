auth:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.auth
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=300
  volumes:
    - auth_data:/var/lib/data
  restart: unless-stopped
