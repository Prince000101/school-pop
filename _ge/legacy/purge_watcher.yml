repo:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.repo
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=3600
  volumes:
    - repo_data:/var/lib/data
  restart: unless-stopped
