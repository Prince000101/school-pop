route:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.route
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=600
  volumes:
    - route_data:/var/lib/data
  restart: unless-stopped
