navbar:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.navbar
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=600
  volumes:
    - navbar_data:/var/lib/data
  restart: unless-stopped
