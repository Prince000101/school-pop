utils:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.utils
    args:
      NODE_ENV: production
  ports:
    - "4000:4000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=60
  volumes:
    - utils_data:/var/lib/data
  restart: unless-stopped
