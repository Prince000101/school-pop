metrics:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.metrics
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=120
  volumes:
    - metrics_data:/var/lib/data
  restart: unless-stopped
