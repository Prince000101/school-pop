navbar:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.navbar
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=300
  volumes:
    - navbar_data:/var/lib/data
  restart: unless-stopped
