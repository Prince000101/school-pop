table:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.table
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=120
  volumes:
    - table_data:/var/lib/data
  restart: unless-stopped
