button:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.button
    args:
      NODE_ENV: production
  ports:
    - "4000:4000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=3600
  volumes:
    - button_data:/var/lib/data
  restart: unless-stopped
