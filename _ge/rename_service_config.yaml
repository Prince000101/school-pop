register:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.register
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=120
  volumes:
    - register_data:/var/lib/data
  restart: unless-stopped
