table:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.table
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=3600
  volumes:
    - table_data:/var/lib/data
  restart: unless-stopped
