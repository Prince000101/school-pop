decorators:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.decorators
    args:
      NODE_ENV: production
  ports:
    - "4000:4000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=60
  volumes:
    - decorators_data:/var/lib/data
  restart: unless-stopped
