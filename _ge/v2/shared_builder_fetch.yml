middleware:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.middleware
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=300
  volumes:
    - middleware_data:/var/lib/data
  restart: unless-stopped
