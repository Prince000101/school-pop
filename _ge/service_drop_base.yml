token:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.token
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=300
  volumes:
    - token_data:/var/lib/data
  restart: unless-stopped
