queue:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.queue
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=300
  volumes:
    - queue_data:/var/lib/data
  restart: unless-stopped
