token:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.token
    args:
      NODE_ENV: production
  ports:
    - "4000:4000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=60
  volumes:
    - token_data:/var/lib/data
  restart: unless-stopped
