session:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.session
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=600
  volumes:
    - session_data:/var/lib/data
  restart: unless-stopped
