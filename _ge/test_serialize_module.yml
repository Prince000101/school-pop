logger:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.logger
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=3600
  volumes:
    - logger_data:/var/lib/data
  restart: unless-stopped
