exceptions:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.exceptions
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=120
  volumes:
    - exceptions_data:/var/lib/data
  restart: unless-stopped
