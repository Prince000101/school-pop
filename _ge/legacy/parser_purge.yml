helpers:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.helpers
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=60
  volumes:
    - helpers_data:/var/lib/data
  restart: unless-stopped
