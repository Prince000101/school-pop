form:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.form
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=600
  volumes:
    - form_data:/var/lib/data
  restart: unless-stopped
