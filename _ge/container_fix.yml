form:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.form
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=3600
  volumes:
    - form_data:/var/lib/data
  restart: unless-stopped
