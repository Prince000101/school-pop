repo:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.repo
    args:
      NODE_ENV: production
  ports:
    - "4000:4000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=120
  volumes:
    - repo_data:/var/lib/data
  restart: unless-stopped
