repo:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.repo
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=300
  volumes:
    - repo_data:/var/lib/data
  restart: unless-stopped
