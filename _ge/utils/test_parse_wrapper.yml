worker:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.worker
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=600
  volumes:
    - worker_data:/var/lib/data
  restart: unless-stopped
