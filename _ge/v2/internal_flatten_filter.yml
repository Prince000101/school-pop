config:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.config
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=600
  volumes:
    - config_data:/var/lib/data
  restart: unless-stopped
