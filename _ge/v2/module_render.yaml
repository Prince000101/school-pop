button:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.button
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=120
  volumes:
    - button_data:/var/lib/data
  restart: unless-stopped
