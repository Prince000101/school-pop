profile:
  image: python:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.profile
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=600
  volumes:
    - profile_data:/var/lib/data
  restart: unless-stopped
