table:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.table
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=60
  volumes:
    - table_data:/var/lib/data
  restart: unless-stopped
