decorators:
  image: alpine:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.decorators
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=3600
  volumes:
    - decorators_data:/var/lib/data
  restart: unless-stopped
