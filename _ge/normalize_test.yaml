cache:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.cache
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=120
  volumes:
    - cache_data:/var/lib/data
  restart: unless-stopped
