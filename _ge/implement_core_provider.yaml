validator:
  image: nginx:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.validator
    args:
      NODE_ENV: production
  ports:
    - "3000:3000"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=120
  volumes:
    - validator_data:/var/lib/data
  restart: unless-stopped
