profile:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.profile
    args:
      NODE_ENV: production
  ports:
    - "8000:8000"
  environment:
    - LOG_LEVEL=WARNING
    - CACHE_TTL=120
  volumes:
    - profile_data:/var/lib/data
  restart: unless-stopped
