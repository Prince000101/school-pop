api:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.api
    args:
      NODE_ENV: production
  ports:
    - "8080:8080"
  environment:
    - LOG_LEVEL=DEBUG
    - CACHE_TTL=120
  volumes:
    - api_data:/var/lib/data
  restart: unless-stopped
