auth:
  image: node:${{TAG}}
  build:
    context: .
    dockerfile: Dockerfile.auth
    args:
      NODE_ENV: production
  ports:
    - "5000:5000"
  environment:
    - LOG_LEVEL=INFO
    - CACHE_TTL=60
  volumes:
    - auth_data:/var/lib/data
  restart: unless-stopped
